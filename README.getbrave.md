`getbrave.cmd`: Download standalone Brave binaries for you. Supports Release, Beta and Nightly channels.
Requires:
- BusyBox (or you can find-and-replace the `busybox wget` part to any downloader you want).  
